#include <stdio.h>
#include <stdbool.h>
#include "Menu.h"
#include "editor.h"

int main(int argc,const char* argv[])
{
    char ch;
    while(true)
    {
        ch = printMenu_A();

        switch(ch)
        {
            case '1':break;
            case '2':break;
        }
    }
}